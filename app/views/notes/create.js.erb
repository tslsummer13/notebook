<% publish_to "/notes/create" do %>
  var note_element = $('<%= escape_javascript(render 'note', :note => @note) %>');
  note_element.css("display", "none");
  $('#new_note').parent().before(note_element);
  note_element.slideDown();
<% end %>

$('#new_note #note_content').val('');
