<ul>
  <% @notes.each do |the_note| %>
    <%= render 'note', :note => the_note %>
  <% end %>
  <li>
    <%= render 'embedded_form', :note => Note.new %>
  </li>
</ul>

<%= subscribe_to "/notes/create" %>
<%= subscribe_to "/notes/destroy" %>
<%= subscribe_to "/notes/update" %>
